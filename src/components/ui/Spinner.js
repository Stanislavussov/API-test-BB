import React from 'react';
import spinner from '../../img/spinner.gif'

const Spinner = () => {
    return (
        <img src={spinner} style={{width: '180px', margin: 'auto', display: 'block'}}/>
    )
}

export default Spinner;
